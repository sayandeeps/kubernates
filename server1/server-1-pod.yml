apiVersion: v1
kind: Pod
metadata:
  name: server-1-pod

spec:
  containers:
    - name: server-1-ctr
      image: sayandeeps/server1:latest
      ports:
        - containerPort: 8000
      imagePullPolicy: Always

